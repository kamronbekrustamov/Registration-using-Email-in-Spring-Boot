spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: localhost
    port: 1025
    username: hello
    password: hello
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
            auth: true
            starttls:
              enable: true
              connectiontimeout: 5000
              timeout: 3000
              writetimeout: 3000